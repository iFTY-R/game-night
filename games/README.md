# Games

每款游戏在自己的顶层目录维护协议、纯规则引擎、可见性投影、SDK 适配、客户端模块、主题和测试。当前骰子模块为 `liars-dice`、`dice-789` 和 `meet-by-chance`。

游戏不能导入应用入口或 PartyRoom 内部实现。服务端引擎只能依赖服务端 Game SDK 和自身纯规则子包；客户端只能依赖客户端 SDK 与游戏 UI 原语；主题不能改变规则或安全语义。
